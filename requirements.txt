# Core dependencies (lightweight — installs fast)
pydantic>=2.0
pyyaml>=6.0
rich>=13.0
textual>=0.50
structlog>=24.0
platformdirs>=4.0
httpx>=0.27
croniter>=2.0
fastapi>=0.100
uvicorn[standard]>=0.30
